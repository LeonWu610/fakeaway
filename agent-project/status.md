# 当前状态

## 当前工作单元
商家与商品图片补齐及新商品扩充已完成并验证。项目继续处于跨电脑迁移与公网首发准备阶段；下一工作单元应优先完成部署前质量、分享与反馈闭环。

## 当前状态
- 新增 `src/utils/relationship.js`，以 `fakeaway.relationships` 按 `restaurantId` 保存来访/完成订单数、按真实数量累计的偏好商品、累计专注秒数、完成任务数、首次/最近相遇时间和最近订单；额外保存已结算订单 ID 用于幂等。
- 关系只在 `DeliveredPage` 结算。相同 `orderId` 刷新或再次进入时不重复增长；localStorage 读取、解析和写入失败均安全降级。
- 没有关系数据时会从旧 `fakeaway.memories` 按 `orderId` 推导基础关系，推导只发生在读取结果中，并用已结算订单集合避免重复累加。
- 阶段为“第一次见面 / 见过几次 / 店里熟客 / 深夜老朋友”，按 0、1-2、3-5、6+ 次完成订单自然成长，不含签到、连续天数或掉级。
- 商家页在商家信息与 tab 之间显示熟客卡；老客可查看次数、常点和等待成果，并用“还是老样子”按默认规格追加偏好商品，不清空购物车、不覆盖组合点单。
- 送达页写入包含 `deliveredAt`、商品数量摘要、等待任务和结算阶段的 memory，并在华丽结算中展示本次是否升阶、第几次、偏好及累计成果。
- 首页在有关系时于时段推荐后显示最多 4 家“常亮的小店”，按最近相遇排序；个人页显示熟店数、累计专注分钟和最多 3 家熟店；历史订单显示当时关系阶段及当前累计等待成果。
- 原有真实等待、轻任务、送达结算、夜宵/饮品和所有工作区未提交改动均保留。
- 5.0 本轮 20 张额度已用完：16 张补齐 r26-r32 历史缺图（10 张隐藏商品图、r27-r32 共 6 张正式封面），4 张用于 r26/r27 新商品。
- 4.5 本轮 6 张额度已全部用于新增饮品商品；共新增 10 个商品，r26/r27 各 10 个商品，r28/r30/r31/r32 各 9 个，r29 为 10 个。
- 已移除缺图商品隐藏和临时商品封面降级；本轮 26 张原图及 52 张 320/640 WebP 变体已同步 R2，远端无待上传对象。

## 验证证据
- 2026-07-22：`npm run lint` 通过。
- 2026-07-22：`npm run build` 通过，Vite 成功转换 68 个模块并生成生产包。
- 2026-07-22：`git diff --check` 通过；本轮编辑文件无 IDE diagnostics。
- 2026-07-22：390×844 浏览器走查通过；隔离 QA 订单首次结算关系为 1 次，刷新送达页后仍为 1 次且 `settledOrderIds` 只有该订单，随后已恢复测试前浏览器存储。
- 2026-07-23：图片生成队列 premium/bulk 均为 0 pending；变体生成 `generated=52 failures=0`；R2 `discovered=921 pending=0`；公网抽检原图和变体均返回 HTTP 200。
- 2026-07-23：`npm run lint`、`npm run build`、`git diff --check` 通过，新增数据无 IDE diagnostics。

## 阻塞或风险
- 当前工作区仍有多项已验收但未提交的修改和新增文件；迁移前必须提交并推送到远程仓库，否则另一台电脑无法通过 Git 获得本轮成果。
- 本地图片二进制已被 `.gitignore` 排除，公网构建必须配置 `VITE_IMAGE_BASE_URL` 指向现有公开 R2 域名；不得提交 `.env.local` 或任何图片生成密钥。
- 此前欠费导致的 16 张缺图已经全部补齐，不再构成风险。

## 下一步
1. 在当前电脑审阅 `git status`，将本轮已验收代码提交并推送至 `origin`；迁移前确认没有密钥或 `.env.local` 进入提交。
2. 在新电脑克隆仓库，安装 Node 依赖，配置 `VITE_IMAGE_BASE_URL`，执行 lint、build 和移动端主链路验证。
3. 首发推荐接入 Cloudflare Pages，并优先完成分享卡、基础访问分析、反馈入口和 PWA 安装体验。

## Changelog
- 2026-07-23：用完 5.0 的 20 张与 4.5 的 6 张额度，补齐 16 张历史缺图并新增 10 个商品，完成变体与 R2 同步。
- 2026-07-22：熟客关系闭环通过用户验收，进入跨电脑迁移与公网首发准备。
- 2026-07-22：完成熟客关系数据契约、送达幂等结算、商家复购入口及首页/订单/个人中心回看闭环。
- 2026-07-22：真实等待、轻任务与华丽送达结算通过用户验收。
