# 当前状态

## 当前工作单元
优先把美食外卖做深做广，并规划消耗 Seedream 4.5 的 200 张免费额度与现有高级模型 50 张额度。R2 图片迁移与前端远端基址切换已完成。下一工作单元是用 Seedream 4.5 生成并接入第一批新增餐饮图片，同时继续按批次同步到 R2。

## 当前状态
- 主链路已覆盖首页、搜索筛选、商家菜单、规格购物车、确认模拟订单、配送等待、送达结算、订单中心、发现页和个人中心。
- 统一商家源现有 16 家餐饮商家：原有 6 家饮品/快餐/咖啡/火锅，第一批 4 家中式正餐，第二批 3 家日料/轻食/早餐，以及本轮 3 家饺子/小炒/烘焙甜点；非餐饮数据仍保留但暂不继续扩充。
- 本轮新增餐饮为：一口到家饺子馆（手工饺子）、灶边小炒（家常小炒）、麦香慢慢（烘焙甜点），共 24 个商品；当前使用品类匹配的远程占位图，不消耗新的 Seedream 额度。
- 新增商家已进入 `allRestaurants.js`，支持首页列表、搜索、排序筛选、商家详情、规格选择和完整下单链路。
- 首页“美食”快捷入口展示全部餐饮；筛选面板新增“饺子小炒”和“烘焙甜点”，可直接定位本轮商家。
- `productProfiles.js` 已新增 dumplings、stirFry、bakery 商品档案：饺子支持做法/蘸料，小炒支持辣度/主食，烘焙支持加热/包装；单位、配料、过敏原、供应时段和制作时间均已适配。
- 图片生成链路已切换为个人火山方舟已授权的 `doubao-seedream-5-0-lite-260128`；`.env.local` 保存 Key 且被 Git 忽略，`.venv` 隔离依赖，`scripts/generate_images.py` 支持单张/批量、数量保护、跳过已有文件、自动识别图片编码，并显式输出无水印 PNG。
- 已成功生成并接入 `霸气草莓奶盖` 原创图：`public/images/products/r1-item1-strawberry-cheese-tea.jpg`。
- `scripts/food_image_batch.json` 现包含 49 张已完成图片任务：原有 40 张，加上 r11-r17 七家第四款商品图，以及 r14 蒜蓉烤茄子、r17 鲜肉小笼包共 9 张。
- r1-r6 首页主图与详情页第一张封面均已切换本地原创图；r1 原有招牌商品图继续保留，r2-r6 首个招牌商品已接入原创图，r3 额外补齐第二款香辣鸡腿堡。
- `generate_images.py` 的断点续传能识别 `.png/.jpg/.jpeg/.webp`；现已增加 bulk/premium 双档位、按档位筛选、单次模型覆盖、dry-run 和分档成本统计。bulk 默认模型已按用户修正为 `doubao-seedream-4-5-251128`，premium 保持原 Seedream 5.0 Lite；输出继续显式关闭水印。
- 图片托管首选 Cloudflare R2 Standard：预计 1-4GB 低于当前 10GB-month 免费层，公开下载免流量费并兼容 S3 API；前端最终应只保存稳定对象 key 或自定义域名 URL。
- `public/images/merchants/*` 与 `public/images/products/*` 已加入 Git 忽略；仓库仅保存数据、prompt 和对象清单。50 个本地图片文件（约 232MB）已全部上传到 R2 的 `images/` 前缀，并生成 `scripts/r2_image_manifest.json`（含 key、公开 URL、大小、SHA-256）。其中 21 个历史文件仍被 Git 跟踪，公开 URL 验证通过后再从索引移除，不删除工作区素材。
- 新增 `scripts/sync_images_to_r2.py`：支持连接校验、远端同大小跳过、`--max-files` 安全阈值、dry-run、缓存头、内容类型和对象清单。

## 验证证据
- `npm run lint` 通过，0 error。
- `npm run build` 通过，Vite 成功构建 61 个模块。
- 数据盘点通过：共 20 家商家、168 个商品；餐饮为 r1-r6 与 r11-r20 共 16 家。现有本地原创封面 13 张、商品图 35 张；全量现有数据还缺 7 张封面和 133 张商品图。
- 双模型 dry-run 验证通过：bulk 正确解析为 `doubao-seedream-4-5-251128`；Python 编译、ESLint、Vite build 与 `git diff --check` 均通过。
- R2 凭证和 Bucket 连接验证通过；远端 `images/` 前缀现有 50 个对象，二次同步显示 pending=0。
- r2.dev 公共开发 URL 已验证返回 HTTP 200、`image/png` 与一年 immutable 缓存头；`allRestaurants.js` 会将所有 `/images/` 路径映射到 `VITE_IMAGE_BASE_URL`，未配置时仍回退本地路径，Unsplash URL 不受影响。
- 本地现有 50 张原创餐饮图片；本批 9 张均通过 PNG 文件头、2048×2048 尺寸与最小文件大小校验，新增商品绑定全部通过。
- 第二批 Seedream 任务成功完成 12/12，脚本正确跳过首批 8 张已有文件。
- 新增数据校验通过：r15-r17 各 8 个商品，商品 ID 唯一，图片字段完整，全部必选规格都有默认值。
- `npm run lint`、`npm run build`、`git diff --check` 通过，Vite 构建 61 个模块。

## 阻塞或风险
- 用户明确说明当前有高级模型 50 张额度，Seedream 4.5 有 200 张免费额度；模型 ID 已修正为 `doubao-seedream-4-5-251128`。每批仍必须通过 `--max-images` 限制调用量。
- R2 S3 凭证已正确配置并完成上传；任何 Access Key/Secret 都不能放入 `VITE_*` 或提交 Git。
- r2.dev 适合当前开发验证，但正式公开发布前仍建议绑定自有图片域名，以便未来切换存储供应商且避免开发域名限流。
- Git 仍跟踪 21 个历史图片二进制；已通过 `.gitignore` 阻止新增图片入库。是否把历史图片从 Git 索引移除应在用户明确要求整理提交时进行，避免擅自改变暂存区。
- 当前仍有大量未提交改动，必须保持增量，不回退用户现有实现。
- 当前 13 家餐饮均已有原创首页封面和至少一个原创招牌商品图；r15-r17 分别接入日料寿司、鸡胸谷物碗、皮蛋瘦肉粥的 2048×2048 原创 PNG。
- 新增 `一尾入魂`（日料）、`草木有饭`（轻食）、`早粥到`（粥品早餐），每家 8 个商品；分别提供佐料、主食/酱汁、份量规格与对应配料、过敏原和供应时段。
- 首页“美食”入口现在展示全部餐饮，不再只筛米饭面食；筛选面板新增“日料轻食”和“粥品早餐”。
- 用户希望长期扩充很多商家和商品，应按批次推进并控制每批可验收边界，避免一次生成大量低质量同质内容。

## 下一步
1. 为 r18-r20 准备第一批 4.5 清单：3 张商家封面 + 24 张商品图，共 27 张；生成后先审核数量、食材与畸变。
2. 将通过审核的图片同步到 R2，更新数据路径并验证完整下单链路；每次同步继续使用 `--max-files`。
3. 之后用 4.5 补齐 r11-r17 后半菜单和 r1-r6 商品缺口；高级 50 张用于新店首屏 hero、封面和失败重做，至少预留 10 张质量修复缓冲。
4. 批量生成以每次会话 3 家、24-40 张为上限。仍以餐饮为主，零售 28 张缺口放到最后。

## Changelog
- 2026-07-17：确定餐饮优先，新增 4 家高频正餐商家和 32 个商品；通过已授权 Seedream 5.0 Lite 分两批完成 20 张原创餐饮图片并接入 10 家餐饮。
- 2026-07-20：用户验收第二批图片和三家新餐饮内容通过；新增日料、轻食、粥品早餐共 24 个商品；分批新增 6+14+9 张图片，补齐 r11-r17 的封面与重点商品图；随后新增饺子、小炒、烘焙甜点 3 家和 24 个商品，餐饮总数为 16 家。
